#!/bin/zsh

function _fuzzy-fs-open-shell {
    local pwd=$*
    if [[ ! -z $TMUX ]]; then
        tmux new-window -c "$pwd" -n "dir $(basename $pwd)"
    elif type "$TERMINAL" &> /dev/null; then
        $TERMINAL --workdir $pwd &> /dev/null &
    else
        $SHELL
    fi
}
